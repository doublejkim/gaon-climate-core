logging:
  level:
    root: DEBUG
    dev.gaonstack.gaonclimatecore: DEBUG
    org.hibernate.SQL: DEBUG

spring:
  jpa:
    properties:
      hibernate:
        format_sql: true

app:
  cors:
    allowed-origins: http://localhost:5173
  climate:
    history:
      window-seconds: 60
      bucket-seconds: 10
